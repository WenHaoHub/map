# 🗺️ 三级地图下钻功能说明

## ✅ 已实现功能

### 三级下钻层级

1. **第一级：中国地图** (`currentLevel: 'china'`)
   - 显示：全国所有省份/直辖市/自治区边界
   - 数据源：`/china.json`
   - 操作：点击任意省份 → 进入第二级

2. **第二级：省份区县地图** (`currentLevel: 'province'`)
   - 显示：该省份下所有区县的边界
   - 数据源：`/province/{省代码}.json`（如 `/province/110000.json` 显示北京所有区县）
   - 操作：点击任意区县 → 进入第三级

3. **第三级：单个区县地图** (`currentLevel: 'district'`)
   - 显示：**仅显示该区县的边界**（不显示其他区县）
   - 数据源：`/county/{区县代码}.json`（如 `/county/110101.json` 仅显示东城区）
   - 操作：已经是最底层，不再下钻

## 🎯 数据文件对应关系

### 数据结构示例

```
public/
├── china.json                 # 全国省份边界
├── province/
│   ├── 110000.json           # 北京市所有区县（东城、西城、朝阳...）
│   ├── 120000.json           # 天津市所有区县
│   ├── 310000.json           # 上海市所有区县
│   └── ...                   # 其他省份（共34个）
└── county/
    ├── 110101.json           # 仅东城区边界 ✅
    ├── 110102.json           # 仅西城区边界 ✅
    ├── 110105.json           # 仅朝阳区边界 ✅
    └── ...                   # 其他区县（共2884个）
```

### 代码对应关系

| adcode | 名称 | 文件位置 | 包含内容 |
|--------|------|----------|----------|
| 110000 | 北京市 | `/province/110000.json` | 北京所有16个区县 |
| 110101 | 东城区 | `/county/110101.json` | **仅东城区边界** |
| 110102 | 西城区 | `/county/110102.json` | **仅西城区边界** |
| 320000 | 江苏省 | `/province/320000.json` | 江苏所有区县 |
| 320100 | 南京市 | `/province/320100.json` | 南京所有区县 |
| 320102 | 玄武区 | `/county/320102.json` | **仅玄武区边界** |

## 🎮 使用演示

### 操作流程

```
1. 打开页面
   ↓ 显示：中国地图（34个省份）
   
2. 点击"北京市"
   ↓ 显示：北京市所有16个区县（东城、西城、朝阳...）
   ↓ 面包屑：中国 > 北京市
   
3. 点击"东城区"
   ↓ 显示：仅东城区的边界 ✅
   ↓ 面包屑：中国 > 北京市 > 东城区
   
4. 点击面包屑"北京市"
   ↓ 返回：北京市所有区县视图
```

## 🔧 核心代码逻辑

### 数据加载函数

```javascript
// 第一级：加载中国地图
loadChinaMap() {
  axios.get('/china.json')
  // 显示所有省份
}

// 第二级：加载省份的所有区县
loadProvinceMap(adcode, name) {
  axios.get(`/province/${adcode}.json`)
  // 显示该省所有区县，如：北京16个区
}

// 第三级：加载单个区县（重点！）
loadDistrictMap(adcode, name) {
  axios.get(`/county/${adcode}.json`)
  // ✅ 只显示该区县边界，如：仅东城区
}
```

### 点击处理逻辑

```javascript
handleMapClick(params) {
  if (currentLevel === 'china') {
    // 点击省份 → 加载该省所有区县
    loadProvinceMap(adcode, name)
  } else if (currentLevel === 'province') {
    // 点击区县 → 加载单个区县边界 ✅
    loadDistrictMap(adcode, name)
  }
  // district 层级不再下钻
}
```

## ✨ 功能特性

- ✅ **三级下钻**：中国 → 省份区县 → 单个区县
- ✅ **精确展示**：第三级只显示单个区县边界，不会显示其他区县
- ✅ **面包屑导航**：支持快速返回任意层级
- ✅ **数据完整**：已复制 2884 个区县 JSON 文件到 public/county/
- ✅ **智能缓存**：每个层级地图独立注册，切换流畅

## 📊 数据验证

### county 文件特征

每个 county 文件只包含**单个区县**的数据：

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "properties": {
        "adcode": 110101,
        "name": "东城区",
        "level": "district",
        "parent": {"adcode": 110000}
      },
      "geometry": {
        "type": "MultiPolygon",
        "coordinates": [...]  // 仅东城区的坐标数据
      }
    }
  ]
}
```

### province 文件特征

每个 province 文件包含**该省所有区县**：

```json
{
  "type": "FeatureCollection",
  "features": [
    {"properties": {"adcode": 110101, "name": "东城区"}, ...},
    {"properties": {"adcode": 110102, "name": "西城区"}, ...},
    {"properties": {"adcode": 110105, "name": "朝阳区"}, ...},
    // ... 更多区县
  ]
}
```

## 🎉 总结

**当前提供的地图数据完全满足你的需求！**

- ✅ `china.json`：全国省份
- ✅ `province/*.json`：各省所有区县
- ✅ `county/*.json`：**单个区县边界**（每个文件只包含一个区县）

你现在可以：
1. 查看全国省份分布
2. 下钻到某省，查看该省所有区县
3. **再下钻到单个区县，只显示该区县的边界** ✨

---

**启动项目测试：**

```bash
cd c:\Users\user\Desktop\GeoMapData_CN-master\demo
npm run dev
```

访问 http://localhost:8081/ 即可体验三级下钻功能！
